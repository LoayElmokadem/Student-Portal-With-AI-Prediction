import express from'express'
import bcrypt from 'bcrypt'

const app = express()
const port = 3000;
const users = [];

app.use(express.json())
app.listen(port,()=>{
    console.log('ANA gEEEEEEETTTTTTTTTTTTTTT');
})